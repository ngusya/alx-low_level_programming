#include "lists.h"

/**
* find_lisint_loop - finds the loop in a linked list
* @head: pointer to a struct
* Return: a number
*/
listint_t *find_listint_loop(listint_t *head)
{
listint_t *headcopy;

headcopy = head;
return (headcopy);
}
