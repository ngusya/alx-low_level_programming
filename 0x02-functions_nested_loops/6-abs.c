/**
* _abs - find absolute value
* @n : number to test
* Description: find absolute value of an int
* Return: returns absolute value of n
*/

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
